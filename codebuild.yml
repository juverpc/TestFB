version: 0.1

phases:

  install:

    commands:
      - sudo npm install
      - sudo npm install -g serverless

  build:

    commands:
      - ./node_modules/.bin/serverless deploy --stage cicd | tee deploy.out

  
 

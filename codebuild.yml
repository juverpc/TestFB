version: 0.1

phases:

  install:

    commands:

      - npm install

      - npm install -g mocha

      - npm install -g serverless

  build:

    commands:

      - ./node_modules/.bin/serverless deploy --stage cicd | tee deploy.out

  
 
